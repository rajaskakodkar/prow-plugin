#! overlay-deployment.yaml

#@ load("@ytt:overlay", "overlay")
#@ load("@ytt:data", "data")

#@overlay/match by=overlay.subset({"kind":"Deployment", "metadata":{"name":"crier_deployment"}})


spec:
  #@overlay/match missing_ok=True
  replicas: #@ data.values.runtime.replicas
